import React from 'react';
import style from './footer.module.css'

const Footer = () => {
  return (
    <div className={`text-center bg-primary ${style.footer}`}>
      Small family happy family
    </div>
  );
}

export default Footer;
